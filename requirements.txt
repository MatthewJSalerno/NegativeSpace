# Exact versions, so an image built today is the image that was validated.
# Update deliberately: change a pin, rebuild, run CI, and validate against a
# real library before merging.
Pillow==12.3.0
pillow-heif==1.7.0
imagehash==4.3.2
rawpy==0.27.1
pyexiftool==0.5.6
# Catalog backups are Zstandard-compressed; bundles libzstd 1.5.7.
zstandard==0.25.0
# The web API (webui/). httpx is FastAPI's test client, used by tests/webui_api_test.py.
fastapi==0.141.1
uvicorn==0.53.0
websockets==17.1
httpx==0.28.1

# Pulled in by the packages above; pinned for the same reason.
numpy==2.4.6
scipy==1.17.1
PyWavelets==1.9.0
packaging==26.3
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.15.1
certifi==2026.7.22
click==8.5.0
h11==0.16.0
httpcore==1.0.9
idna==3.20
pydantic==2.13.5
pydantic_core==2.46.5
starlette==1.7.0
typing-inspection==0.4.4
typing_extensions==4.16.0
